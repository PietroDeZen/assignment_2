
   






// the statement circleX +1 is evaluated // circleX = 100 // circleX = circleX + 1 creates a loop
// the variable is constantly updated // the loop won't stop if no intervention takes place


